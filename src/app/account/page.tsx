// Личный кабинет → «Профиль». Данные клиента берём из клиентского контекста (ProfileForm).
import { ProfileForm } from "@/components/account/profile-form";

export default function AccountProfilePage() {
  return <ProfileForm />;
}
